# Jolt 布娃娃系统调查报告 — Part 3: 布娃娃配置与稳定化

## 一、RagdollSettings — 布娃娃配置

**源文件**: `Jolt/Physics/Ragdoll/Ragdoll.h` (前半部分)

RagdollSettings 是布娃娃的**蓝图/模板**，描述所有肢体的形状、质量、约束参数。可以从一个 Settings 创建多个 Ragdoll 实例。

### 1.1 核心结构

```cpp
class RagdollSettings {
    Ref<Skeleton>  mSkeleton;    // 引用的骨骼
    PartVector     mParts;       // 与 skeleton.joints 一一对应
    
    // 额外约束（非父子关系的约束，如手腕绑定等）
    AdditionalConstraintVector mAdditionalConstraints;
};
```

### 1.2 Part — 单个肢体定义

Part **继承自** `BodyCreationSettings`，即包含创建一个刚体所需的全部信息，外加一个连接到父关节的约束设置：

```cpp
class Part : public BodyCreationSettings {
    // 继承自 BodyCreationSettings 的:
    //   Shape*       shape;          // 碰撞形状
    //   RVec3        position;       // 初始位置
    //   Quat         rotation;       // 初始朝向
    //   EMotionType  motionType;     // Dynamic / Kinematic / Static
    //   ObjectLayer  objectLayer;    // 碰撞层
    //   ... 质量、摩擦、弹性 等
    
    Ref<TwoBodyConstraintSettings> mToParent;  // 连接到父关节的约束
    // 根关节(index=0) 的 mToParent 为 nullptr
};
```

### 1.3 AdditionalConstraint — 额外约束

用于非父子关系的关节连接（例如骨盆两侧的交叉约束）：

```cpp
class AdditionalConstraint {
    int mBodyIdx[2];                        // 连接的两个 Part 索引
    Ref<TwoBodyConstraintSettings> mConstraint;  // 约束设置
};
```

---

## 二、从代码创建布娃娃：完整示例

来自 `RagdollLoader::sCreate()` 的完整创建流程：

### 2.1 步骤 1：创建骨骼

```cpp
Ref<Skeleton> skeleton = new Skeleton;
uint lower_body = skeleton->AddJoint("LowerBody");           // 根
uint mid_body   = skeleton->AddJoint("MidBody", lower_body);
uint upper_body = skeleton->AddJoint("UpperBody", mid_body);
uint head       = skeleton->AddJoint("Head", upper_body);
uint upper_arm_l = skeleton->AddJoint("UpperArmL", upper_body);
uint upper_arm_r = skeleton->AddJoint("UpperArmR", upper_body);
uint lower_arm_l = skeleton->AddJoint("LowerArmL", upper_arm_l);
uint lower_arm_r = skeleton->AddJoint("LowerArmR", upper_arm_r);
uint upper_leg_l = skeleton->AddJoint("UpperLegL", lower_body);
uint upper_leg_r = skeleton->AddJoint("UpperLegR", lower_body);
uint lower_leg_l = skeleton->AddJoint("LowerLegL", upper_leg_l);
uint lower_leg_r = skeleton->AddJoint("LowerLegR", upper_leg_r);
```

### 2.2 步骤 2：定义形状

每个肢体都用 **CapsuleShape** (半径+半高)：

```
LowerBody:  CapsuleShape(半高=0.15, 半径=0.10)
MidBody:    CapsuleShape(半高=0.15, 半径=0.10)
UpperBody:  CapsuleShape(半高=0.15, 半径=0.10)
Head:       CapsuleShape(半高=0.075, 半径=0.10)
UpperArmL/R: CapsuleShape(半高=0.15, 半径=0.06)
LowerArmL/R: CapsuleShape(半高=0.15, 半径=0.05)
UpperLegL/R: CapsuleShape(半高=0.2,  半径=0.075)
LowerLegL/R: CapsuleShape(半高=0.2,  半径=0.06)
```

### 2.3 步骤 3：设置位置和朝向

```
身体部位：        位置 (世界空间)           朝向
LowerBody:       (0, 1.15, 0)            绕Z轴旋转90° (横卧胶囊)
MidBody:         (0, 1.35, 0)            绕Z轴旋转90°
UpperBody:       (0, 1.55, 0)            绕Z轴旋转90°
Head:            (0, 1.825, 0)           无旋转 (竖直胶囊)
UpperArmL:       (-0.425, 1.55, 0)       绕Z轴旋转90°
UpperArmR:       (0.425, 1.55, 0)        绕Z轴旋转90°
LowerArmL:       (-0.8, 1.55, 0)         绕Z轴旋转90°
LowerArmR:       (0.8, 1.55, 0)          绕Z轴旋转90°
UpperLegL:       (-0.15, 0.8, 0)         无旋转
UpperLegR:       (0.15, 0.8, 0)          无旋转
LowerLegL:       (-0.15, 0.3, 0)         无旋转
LowerLegR:       (0.15, 0.3, 0)          无旋转
```

### 2.4 步骤 4：配置约束

每个非根关节通过 `SwingTwistConstraintSettings` 连接到父关节：

```
关节           约束位置              twist轴      twist角  normal角  plane角
MidBody        (0, 1.25, 0)         Y轴向上       ±5°      10°      10°
UpperBody      (0, 1.45, 0)         Y轴向上       ±5°      10°      10°
Head           (0, 1.65, 0)         Y轴向上       ±90°     45°      45°
UpperArmL      (-0.225, 1.55, 0)    -X(向左)      ±45°     90°      45°
UpperArmR      (0.225, 1.55, 0)     +X(向右)      ±45°     90°      45°
LowerArmL      (-0.65, 1.55, 0)     -X            ±45°     0°       90°
LowerArmR      (0.65, 1.55, 0)      +X            ±45°     0°       90°
UpperLegL      (-0.15, 1.05, 0)     -Y(向下)      ±45°     45°      45°
UpperLegR      (0.15, 1.05, 0)      -Y            ±45°     45°      45°
LowerLegL      (-0.15, 0.55, 0)     -Y            ±45°     0°       60°
LowerLegR      (0.15, 0.55, 0)      -Y            ±45°     0°       60°
```

注意：
- 约束位置 = 两个肢体之间的关节点（世界空间）
- `twist轴` = 沿着骨骼延伸的方向
- `normal角=0°` 意味着该方向上不能摆动（像膝盖/肘部只能弯曲不能侧向）
- `plane角` 控制另一个方向的摆动

### 2.5 步骤 5：后处理

```cpp
settings->Stabilize();                      // 稳定化质量/惯性
settings->DisableParentChildCollisions();    // 禁用父子碰撞
settings->CalculateBodyIndexToConstraintIndex(); // 建立索引映射
```

---

## 三、Stabilize() — 稳定化算法

**源文件**: `Ragdoll.cpp` 中的 `RagdollSettings::Stabilize()`

这是来自 Havok 的技术（Oliver Strunk, "Stop my Constraints from Blowing Up!"），解决布娃娃的**约束爆炸**问题。

### 3.1 问题

当父肢体很轻而子肢体很重时（例如手指很重但手腕很轻），约束求解器会变得不稳定，产生剧烈抖动。

### 3.2 算法（两步）

**步骤 1：限制父子质量比**

```
对每条从根到叶的肢体链:
    cMinMassRatio = 0.8
    cMaxMassRatio = 1.2
    
    // 计算每个肢体的目标质量比
    for 每个子肢体 i:
        ratio = mass[child] / mass[parent]
        mass_ratios[i] = mass_ratios[parent] * clamp(ratio, 0.8, 1.2)
    
    // 重新分配质量（保持总质量不变）
    totalMass = sum(所有肢体质量)
    ratio_to_mass = totalMass / sum(mass_ratios)
    
    for 每个肢体 i:
        new_mass = mass_ratios[i] * ratio_to_mass
        inertia *= new_mass / old_mass   // 惯性等比缩放
        mass = new_mass
```

**步骤 2：增大父关节惯性**

```
cMaxInertiaIncrease = 2.0

// 对每个肢体，计算其所有子肢体的惯性之和
for 每个肢体 i (从叶到根遍历):
    childSum[parent] += principalInertia[i].x + childSum[i]

// 确保父关节惯性 ≥ 子关节惯性之和（有上限）
for 每个肢体 i:
    if childSum[i] > 0:
        minimum = min(2.0 * principalInertia[i].x, childSum[i])
        principalInertia[i] = max(principalInertia[i], minimum)
```

### 3.3 要点

- 保持**总质量守恒**（只做重新分配）
- 通过 `MassProperties::DecomposePrincipalMomentsOfInertia()` 分解惯性张量
- Static 刚体不参与质量重分配

---

## 四、DisableParentChildCollisions() — 碰撞过滤

### 4.1 基本过滤

创建 `GroupFilterTable(jointCount)`，对每对父子关节禁用碰撞：

```cpp
for each joint:
    if has parent:
        groupFilter->DisableCollision(jointIdx, parentJointIdx)
```

### 4.2 基于几何的自动过滤

如果提供了初始姿态（`inJointMatrices`），还会自动检测哪些非父子关节在初始状态下已经重叠，并禁用它们之间的碰撞：

```cpp
for each pair (j1, j2):
    if collision still enabled:
        // 用 CollisionDispatch 检测两个 Shape 是否重叠
        if shapes overlap (with separation < minSeparationDistance):
            groupFilter->DisableCollision(j1, j2)
```

### 4.3 分组

每个 Part 被赋予：
- `SubGroupID` = 关节索引
- `GroupFilter` = 共享的 GroupFilterTable

在 `CreateRagdoll()` 时设置 `GroupID`（每个布娃娃实例一个唯一ID），这样**不同布娃娃之间仍然碰撞**，只有同一布娃娃内的特定肢体不碰撞。
